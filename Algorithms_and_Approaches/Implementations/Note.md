It is potentially wise to implement these things in one language, and ask a Language Model to translate the implementation.
One version that is a niave, relatively direct translation from one language to another.
And then another version which operates the same but takes advantage of features and idioms in the language, and comment about these idioms.
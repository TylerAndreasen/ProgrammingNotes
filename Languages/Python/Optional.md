# Optional

The `Optional` type hint is a Python shorthand for indicating that the object being pointed at is could be of the type specified or `None`:

```python
# Taken from LeetCode # 133 Clone Graph
def cloneGraph(self, node: Optional['Node']) -> Optional['Node']:
```
